@model site

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@*<script>

    $(function () {
        $('.ui.dropdown').dropdown();
    });
</script>

<div class="ui selection dropdown">
    <input type="hidden" name="gender">
    <i class="dropdown icon"></i>
    <div class="default text">Select Gender</div>
    <div class="menu">
        <div class="item" data-value="0">Male</div>
        <div class="item" data-value="1">Female</div>
        <div class="item" data-value="2">Other</div>
    </div>
</div>*@

<h2>Добавление новой соц.сети</h2>
<br />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
<div class="ui form">

    @Html.DropDownListFor(model => model.networkicon, ViewBag.Icons as SelectList)

    @*<div class="col-md-10">

        @foreach (networkicon c in ViewBag.Icons)
        {@Html.RadioButtonFor(model => model.networkicon, @c);

        <i class="large @c.ico"></i>

    }
    </div>*@
    @*<input name="networkicon_id" type="radio" value="@c.id" />
        <i class="large @c.ico"></i>
        @c.name*@
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <br/>
        <label>Ссылка</label>
        <div class="col-md-10">
            @Html.EditorFor(model => model.url, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.url, "", new { @class = "text-danger" })
        </div>

        <br/>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Готово" class="btn btn-default" />
            </div>
        </div>
    </div>
</div>
    }
