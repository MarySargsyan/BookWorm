@model site

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<h2>Добавление новой соц.сети</h2>
<br />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
<div class="ui form">

    <select name="network" class="form-control form-control-lg">
        @foreach (networkicon networkicon in ViewBag.Icons)
        {
            <option value="@networkicon.id">
                @networkicon.name
            </option>

        }
    </select>


    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <br />
        <label>Ссылка</label>
        <div class="col-md-10">
            @Html.EditorFor(model => model.url, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.url, "", new { @class = "text-danger" })
        </div>

        <br />
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Готово" class="btn btn-default" />
            </div>
        </div>
    </div>
</div>
    }
